# Locale Checklist

- [x] Keep `Czech Republic` as an active locale in the footer and switcher.
- [x] Keep `Middle East` as stale footer text only.
- [ ] Remove the remaining dead `ar` locale references from app code and content helpers.
- [ ] Remove the unused Arabic block from `src/lib/translations.ts`.
- [ ] Remove the unused Arabic block from `src/lib/seo/keyword-map.ts`.
- [ ] Finish translating non-hero homepage sections for `cz`, `nl`, and `dk`.
- [ ] Translate locale-specific CTA / blog card labels where still hardcoded in English.
- [ ] Run a build or type check after locale cleanup and fix any fallout.

---
phase: 10-belfast-blog-content-strategy
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - src/content/pillars/uk/seo-belfast.mdx
  - src/content/pillars/uk/social-media-belfast.mdx
  - src/content/pillars/uk/paid-ads-belfast.mdx
  - src/content/pillars/uk/website-design-belfast.mdx
  - src/app/sitemap.ts
autonomous: true

must_haves:
  truths:
    - "Each Belfast pillar page lists all its actual cluster posts in clusterPages frontmatter"
    - "Pillar pages reference specific cluster posts by name within their content"
    - "Sitemap includes all new Belfast blog posts with appropriate priority"
    - "Build succeeds with correct total page count reflecting all new content"
    - "Internal linking is bidirectional: pillar links to clusters AND clusters link to pillar"
  artifacts:
    - path: "src/content/pillars/uk/seo-belfast.mdx"
      provides: "Updated clusterPages frontmatter with all SEO cluster slugs"
      contains: "local-seo-belfast"
    - path: "src/content/pillars/uk/social-media-belfast.mdx"
      provides: "Updated clusterPages frontmatter with all Social Media cluster slugs"
      contains: "instagram-marketing-belfast"
    - path: "src/content/pillars/uk/paid-ads-belfast.mdx"
      provides: "Updated clusterPages frontmatter with all Paid Ads cluster slugs"
      contains: "search-ads-belfast"
    - path: "src/content/pillars/uk/website-design-belfast.mdx"
      provides: "Updated clusterPages frontmatter with all Website Design cluster slugs"
      contains: "responsive-website-belfast"
  key_links:
    - from: "src/content/pillars/uk/seo-belfast.mdx"
      to: "src/content/blog/uk/local-seo-belfast.mdx"
      via: "clusterPages frontmatter array"
      pattern: "local-seo-belfast"
    - from: "src/content/blog/uk/local-seo-belfast.mdx"
      to: "src/content/pillars/uk/seo-belfast.mdx"
      via: "pillarSlug frontmatter + content links"
      pattern: "seo-belfast"
---

<objective>
Wire up pillar-cluster relationships, verify bidirectional linking, and validate the complete Belfast content ecosystem.

Purpose: After Plans 10-02 through 10-04 created pillar content and cluster posts, this plan ensures they are properly connected. Pillar pages must list their cluster posts in frontmatter (for the RelatedClusters component), and in-content references to clusters should use real post titles and links. This completes the topical authority architecture.

Output: Updated pillar frontmatter, verified bidirectional links, clean build with correct page count.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/content.ts
@src/lib/blog.ts
@src/content/pillars/uk/seo-belfast.mdx
@src/content/pillars/uk/social-media-belfast.mdx
@src/content/pillars/uk/paid-ads-belfast.mdx
@src/content/pillars/uk/website-design-belfast.mdx
@.planning/phases/10-belfast-blog-content-strategy/10-02-SUMMARY.md
@.planning/phases/10-belfast-blog-content-strategy/10-03-SUMMARY.md
@.planning/phases/10-belfast-blog-content-strategy/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pillar clusterPages frontmatter and in-content cluster references</name>
  <files>src/content/pillars/uk/seo-belfast.mdx, src/content/pillars/uk/social-media-belfast.mdx, src/content/pillars/uk/paid-ads-belfast.mdx, src/content/pillars/uk/website-design-belfast.mdx</files>
  <action>
  Read each Belfast pillar page. Update the `clusterPages` frontmatter array to include ALL cluster blog post slugs that were created in Plans 10-03 and 10-04.

  **seo-belfast.mdx clusterPages update:**
  Current: `["local-seo-belfast", "small-business-seo-belfast", "technical-seo-belfast", "link-building-belfast"]`
  These should already match the posts created in 10-03. Verify they match exactly. If any additional SEO clusters were added in 10-01 keyword map expansion (seo-cost-belfast, how-to-rank-on-google-belfast), add those slugs to the array even though the posts don't exist yet -- the RelatedClusters component gracefully handles missing posts (returns empty array).

  Actually, ONLY list slugs for posts that actually exist as MDX files. The getRelatedClusters function in content.ts calls getPostBySlug which returns null for missing posts. So only include slugs that have corresponding MDX files:
  ```yaml
  clusterPages:
    - "local-seo-belfast"
    - "small-business-seo-belfast"
    - "technical-seo-belfast"
    - "link-building-belfast"
  ```

  **social-media-belfast.mdx clusterPages update:**
  Current: `["instagram-marketing-belfast", "facebook-marketing-belfast"]`
  These should already match the posts created in 10-03. Verify match.

  **paid-ads-belfast.mdx clusterPages update:**
  Current: `["ppc-belfast", "google-ads-cost-belfast"]`
  Update to include the third cluster post from 10-04:
  ```yaml
  clusterPages:
    - "ppc-belfast"
    - "google-ads-cost-belfast"
    - "search-ads-belfast"
  ```

  **website-design-belfast.mdx clusterPages update:**
  Current: `[]` (empty - no clusters were planned when pillar was created in Phase 7)
  Update to include all 3 Website Design clusters from 10-04:
  ```yaml
  clusterPages:
    - "responsive-website-belfast"
    - "ecommerce-website-belfast"
    - "web-design-trends-belfast"
  ```

  **In-content cluster references:**
  Within the body content of each pillar page, where service subsections reference topics covered by cluster posts, add natural inline links to the cluster blog posts. For example:

  In seo-belfast.mdx "Local SEO Belfast" section, add a sentence like:
  "For a detailed guide, read our [complete local SEO guide for Belfast businesses](/uk/blog/local-seo-belfast/)."

  In seo-belfast.mdx "Link Building & Citations" section:
  "Explore our [Belfast link building strategies guide](/uk/blog/link-building-belfast/) for detailed tactics."

  In paid-ads-belfast.mdx "Search Ads Belfast" section:
  "Learn more in our [guide to Google Search Ads for Belfast businesses](/uk/blog/search-ads-belfast/)."

  Add 2-4 such inline cluster references per pillar page, placed naturally within relevant subsections. These create additional internal links from pillar to cluster (strengthening the topical authority model).

  **Do NOT change:**
  - relatedPillars frontmatter (those are pillar-to-pillar links, separate concern)
  - The Phase 9 cross-link sections ("Our Complete X Offering")
  - The Belfast-specific CTA at the bottom
  - Any other frontmatter fields (title, excerpt, date, etc.)
  </action>
  <verify>
  1. seo-belfast.mdx clusterPages has 4 entries matching actual blog post files
  2. social-media-belfast.mdx clusterPages has 2 entries matching actual blog post files
  3. paid-ads-belfast.mdx clusterPages has 3 entries (was 2, now includes search-ads-belfast)
  4. website-design-belfast.mdx clusterPages has 3 entries (was 0, now includes all 3 clusters)
  5. Each pillar contains 2-4 inline links to its cluster blog posts within content
  6. relatedPillars frontmatter unchanged in all files
  7. `npm run build` succeeds
  </verify>
  <done>
  All 4 Belfast pillar pages have correct clusterPages frontmatter and inline cluster references. Bidirectional linking verified: clusters link up to pillars (pillarSlug + content links) and pillars link down to clusters (clusterPages + inline links).
  </done>
</task>

<task type="auto">
  <name>Task 2: Full build verification and Phase 10 content audit</name>
  <files>src/app/sitemap.ts</files>
  <action>
  Run a comprehensive verification of the Phase 10 content ecosystem.

  **Step 1: Verify sitemap includes new blog posts.**
  The sitemap.ts already dynamically generates entries from `getPostSlugs()`, so new blog posts should be automatically included. Verify by:
  - Check that `src/app/sitemap.ts` uses `getPostSlugs(locale)` for blog entries (it does - confirmed in reading)
  - Blog posts in `src/content/blog/uk/` will be auto-detected

  If the sitemap does NOT already set appropriate priority for blog posts, check the current blog priority value and confirm it's reasonable (0.6 is standard for blog posts).

  **Step 2: Run full build and count pages.**
  ```bash
  npm run build
  ```
  Expected: Page count should be 239 (baseline) + 12 new blog posts (for UK locale). Since blog posts fall back from AU/IE to US, and UK has its own posts, the exact count depends on whether UK-only posts generate separate UK pages or not. The blog route uses `generateStaticParams` which iterates all locales and their slugs, so UK-specific cluster posts will only appear under /uk/ path. Verify build succeeds and page count increases.

  **Step 3: Verify bidirectional linking audit.**
  For each Belfast pillar, verify:
  - Grep pillar MDX for cluster post links (pillar -> cluster)
  - Grep each cluster MDX for pillar links (cluster -> pillar)
  - Grep each cluster MDX for /uk/belfast/ link (cluster -> location hub)

  Run these verification commands:
  ```bash
  # Pillar -> Cluster links
  grep -c "/uk/blog/" src/content/pillars/uk/seo-belfast.mdx
  grep -c "/uk/blog/" src/content/pillars/uk/social-media-belfast.mdx
  grep -c "/uk/blog/" src/content/pillars/uk/paid-ads-belfast.mdx
  grep -c "/uk/blog/" src/content/pillars/uk/website-design-belfast.mdx

  # Cluster -> Pillar links (each should have 2+)
  grep -c "/uk/seo-belfast/" src/content/blog/uk/local-seo-belfast.mdx
  grep -c "/uk/seo-belfast/" src/content/blog/uk/small-business-seo-belfast.mdx
  # ... etc for all 12 cluster posts

  # Cluster -> Location hub links (each should have 1+)
  grep -c "/uk/belfast/" src/content/blog/uk/local-seo-belfast.mdx
  # ... etc for all 12 cluster posts
  ```

  **Step 4: Verify cluster metadata in frontmatter.**
  For all 12 new blog posts:
  ```bash
  grep "type:" src/content/blog/uk/local-seo-belfast.mdx
  grep "pillarSlug:" src/content/blog/uk/local-seo-belfast.mdx
  grep "relatedClusters:" src/content/blog/uk/local-seo-belfast.mdx
  # ... etc for all 12
  ```

  **Step 5: Verify no placeholder text remains.**
  ```bash
  grep -r "CONTENT TO BE WRITTEN" src/content/pillars/uk/
  ```
  Expected: Zero matches.

  **Step 6: TypeScript verification.**
  ```bash
  npx tsc --noEmit
  ```
  Expected: Zero errors.

  If sitemap needs any adjustment (unlikely since it's dynamic), make minimal changes. Do NOT refactor the sitemap - only fix if something is broken.

  Document all verification results for the summary.
  </action>
  <verify>
  1. `npm run build` succeeds with increased page count
  2. `npx tsc --noEmit` passes with zero errors
  3. Zero "CONTENT TO BE WRITTEN" placeholders remain in pillar pages
  4. All 12 cluster posts have type: cluster and pillarSlug in frontmatter
  5. All 12 cluster posts link to their parent pillar page (2+ times each)
  6. All 12 cluster posts link to /uk/belfast/ (1+ times each)
  7. All 4 pillar pages link to their cluster posts (2-4 inline links + clusterPages frontmatter)
  8. Bidirectional linking complete: pillars <-> clusters <-> location hub
  </verify>
  <done>
  Phase 10 content ecosystem fully verified. 4 complete pillar pages + 12 cluster blog posts + bidirectional internal linking operational. Build succeeds. TypeScript clean. Zero placeholder content remaining. Topical authority architecture complete for all 4 Belfast service pillars.
  </done>
</task>

</tasks>

<verification>
- Full build passes: `npm run build`
- TypeScript clean: `npx tsc --noEmit`
- Zero placeholders: `grep -r "CONTENT TO BE WRITTEN" src/content/pillars/uk/` returns nothing
- 12 cluster posts all have correct pillarSlug metadata
- Bidirectional links verified between all pillars and their clusters
- Sitemap includes all new blog pages
- Page count reflects 12 new Belfast blog posts
</verification>

<success_criteria>
1. Complete pillar-cluster topical authority architecture verified
2. All pillar clusterPages frontmatter arrays match actual blog post files
3. Bidirectional internal linking: pillar <-> cluster <-> location hub
4. Clean build with increased page count
5. Zero placeholder text, zero TypeScript errors
6. Phase 10 content ready for indexing and distribution
</success_criteria>

<output>
After completion, create `.planning/phases/10-belfast-blog-content-strategy/10-05-SUMMARY.md`
</output>

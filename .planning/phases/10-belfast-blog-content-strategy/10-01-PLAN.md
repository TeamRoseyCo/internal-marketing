---
phase: 10-belfast-blog-content-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/blog.ts
  - src/lib/content.ts
  - src/lib/seo/keyword-map.ts
  - .planning/phases/10-belfast-blog-content-strategy/belfast-content-calendar.md
autonomous: true

must_haves:
  truths:
    - "BlogPost and BlogPostMeta interfaces support cluster metadata fields (type, pillarSlug, relatedClusters)"
    - "Existing blog posts continue to load correctly with no regressions"
    - "Content calendar documents 24-30 Belfast blog topics mapped to 4 pillars with target keywords"
    - "Keyword map UK section contains all planned cluster slugs from topical clusters research"
  artifacts:
    - path: "src/lib/blog.ts"
      provides: "Extended BlogPost interface with optional cluster fields"
      contains: "pillarSlug"
    - path: "src/lib/content.ts"
      provides: "Unchanged pillar loading (no modifications needed)"
    - path: "src/lib/seo/keyword-map.ts"
      provides: "Complete UK keyword map with all planned Belfast cluster entries"
      contains: "instagram-marketing-belfast"
    - path: ".planning/phases/10-belfast-blog-content-strategy/belfast-content-calendar.md"
      provides: "Content calendar with 24-30 planned Belfast blog topics"
      min_lines: 100
  key_links:
    - from: "src/lib/blog.ts"
      to: "src/content/blog/uk/*.mdx"
      via: "gray-matter frontmatter parsing"
      pattern: "pillarSlug|relatedClusters"
    - from: "src/lib/seo/keyword-map.ts"
      to: "src/content/blog/uk/"
      via: "cluster slug mapping"
      pattern: "slug.*belfast"
---

<objective>
Extend blog infrastructure to support pillar-cluster content model and create comprehensive Belfast content calendar.

Purpose: Blog posts need cluster metadata (type, pillarSlug, relatedClusters) to establish pillar-cluster relationships. The keyword map needs all planned cluster entries. A content calendar provides the strategic roadmap for Phase 10 content creation.

Output: Extended blog types, complete keyword map entries, and a 24-30 topic content calendar mapped to Belfast pillars.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/blog.ts
@src/lib/content.ts
@src/lib/seo/keyword-map.ts
@.planning/phases/06-belfast-seo-research-a-strategy/belfast-keyword-map.md
@.planning/phases/06-belfast-seo-research-a-strategy/belfast-topical-clusters.md
@.planning/phases/10-belfast-blog-content-strategy/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend blog types and keyword map for cluster content</name>
  <files>src/lib/blog.ts, src/lib/seo/keyword-map.ts</files>
  <action>
  **Part A: Extend BlogPost and BlogPostMeta interfaces in src/lib/blog.ts**

  Add optional cluster metadata fields to both interfaces:
  - `type?: 'standalone' | 'cluster'` (default behavior is standalone if omitted)
  - `pillarSlug?: string` (links cluster post to parent pillar page slug)
  - `relatedClusters?: string[]` (slugs of related cluster posts for lateral linking)

  Update the `getAllPosts` and `getPostBySlug` functions to parse these fields from frontmatter:
  - In the map function of `getAllPosts`, add: `type: data.type || 'standalone'`, `pillarSlug: data.pillarSlug`, `relatedClusters: data.relatedClusters || []`
  - In `getPostBySlug`, add same fields to the return object

  IMPORTANT: These fields are OPTIONAL. All existing blog posts that lack these fields must continue to work exactly as before. Do NOT make any field required. Use `data.type || undefined` pattern so existing posts without `type` field just get undefined.

  Actually, simplify: keep fields optional (use `?`), only set them if `data.X` exists:
  ```typescript
  // In BlogPostMeta and BlogPost interfaces:
  type?: 'standalone' | 'cluster';
  pillarSlug?: string;
  relatedClusters?: string[];
  ```

  In the parsing, only include if present:
  ```typescript
  ...(data.type && { type: data.type }),
  ...(data.pillarSlug && { pillarSlug: data.pillarSlug }),
  ...(data.relatedClusters && { relatedClusters: data.relatedClusters }),
  ```

  **Part B: Expand UK keyword map in src/lib/seo/keyword-map.ts**

  The UK keyword map currently has 8 cluster entries across 4 pillars. Expand based on the Belfast topical clusters research (belfast-topical-clusters.md). Add these cluster entries:

  SEO pillar (add to existing 4):
  - `seo-cost-belfast` (informational, "SEO cost Belfast")
  - `how-to-rank-on-google-belfast` (informational, "how to rank on Google Belfast")

  Social Media pillar (add to existing 2):
  - `tiktok-marketing-belfast` (commercial, "TikTok marketing Belfast")
  - `linkedin-marketing-belfast` (commercial, "LinkedIn marketing Belfast")
  - `social-media-strategy-belfast` (informational, "social media strategy Belfast")

  Paid Ads pillar (add to existing 2):
  - `search-ads-belfast` (commercial, "search ads Belfast")
  - `shopping-ads-belfast` (commercial, "shopping ads Belfast")
  - `google-ads-management-belfast` (commercial, "Google Ads management Belfast")

  Website Design pillar (currently empty, add):
  - `responsive-website-belfast` (commercial, "responsive website Belfast")
  - `ecommerce-website-belfast` (commercial, "ecommerce website Belfast")
  - `web-design-trends-belfast` (informational, "web design trends Belfast 2026")

  Include secondaryKeywords for each cluster entry, following the existing pattern. Reference belfast-topical-clusters.md for exact keyword groupings.
  </action>
  <verify>
  1. `npx tsc --noEmit` passes with zero errors
  2. `npm run build` succeeds with same page count (239 pages)
  3. Grep src/lib/blog.ts for "pillarSlug" confirms field exists in interface
  4. Grep src/lib/seo/keyword-map.ts for "instagram-marketing-belfast" confirms entry exists
  5. Count UK cluster entries: should be ~19 total across 4 pillars
  </verify>
  <done>
  BlogPost/BlogPostMeta interfaces include optional type, pillarSlug, relatedClusters fields. UK keyword map contains 19+ cluster entries across 4 pillars. Build succeeds with no regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Belfast content calendar</name>
  <files>.planning/phases/10-belfast-blog-content-strategy/belfast-content-calendar.md</files>
  <action>
  Create a comprehensive Belfast blog content calendar at `.planning/phases/10-belfast-blog-content-strategy/belfast-content-calendar.md`.

  The calendar must contain:

  **Header section:**
  - Title: "Belfast Blog Content Calendar"
  - Phase reference: Phase 10
  - Publishing cadence: 2 posts per week (Monday + Thursday)
  - Total planned: 24-30 topics for first 3 months
  - Pillar balance: Roughly equal distribution across 4 Belfast pillars

  **Content Calendar Table (24-30 entries):**

  Format:
  ```
  | Week | Publish Date | Title | Slug | Pillar | Primary Keyword | Intent | Priority |
  ```

  Map topics from the keyword map and topical clusters research. Prioritize:
  1. High-priority cluster pages first (from keyword map): local-seo-belfast, instagram-marketing-belfast, ppc-belfast, etc.
  2. Then medium-priority pages: technical-seo-belfast, tiktok-marketing-belfast, etc.
  3. Then informational/comparison: seo-cost-belfast, how-to-rank-on-google-belfast, etc.

  Balance across pillars: ~7-8 posts per pillar over 3 months.

  **Publishing schedule:**
  - Weeks 1-4: SEO + Social Media pillar clusters (highest priority)
  - Weeks 5-8: Paid Ads + Website Design pillar clusters
  - Weeks 9-12: Informational posts, comparison content, remaining clusters

  **Content workflow section:**
  Document the workflow for each blog post:
  1. Research keyword and competitors (15 min)
  2. Create outline with Belfast-specific angles (30 min)
  3. Write 1000-1500 word MDX post following template (2-3 hours)
  4. Review links, grammar, Belfast authenticity (30 min)
  5. Create MDX file, commit, verify build (15 min)
  6. Distribute: social media + GBP post (30 min)

  **Blog post template section:**
  Include the MDX frontmatter template for Belfast cluster posts:
  ```yaml
  ---
  title: "[SEO-optimized title with Belfast]"
  excerpt: "[One-sentence value prop for Belfast businesses]"
  category: "[SEO|Social Media|Paid Ads|Website Design]"
  type: "cluster"
  pillarSlug: "[parent-pillar-slug]"
  relatedClusters:
    - "[related-post-slug-1]"
    - "[related-post-slug-2]"
  date: "[YYYY-MM-DD]"
  author: "Rosey Co. Team"
  image: "/images/blog/[relevant-image].jpg"
  tags: ["Belfast", "[Service]", "[Topic]"]
  ---
  ```

  And the content structure template showing:
  - Opening with Belfast local hook + pillar page link
  - 3-5 H2 sections with Belfast-specific content
  - Local examples (Cathedral Quarter, Titanic Quarter, BT postcodes)
  - Common mistakes section
  - CTA linking to pillar page and Belfast office
  - Related reading links to other cluster posts

  **Distribution checklist section:**
  Per-post distribution checklist (LinkedIn, Facebook, Instagram, GBP post, email newsletter queue).
  </action>
  <verify>
  1. File exists at .planning/phases/10-belfast-blog-content-strategy/belfast-content-calendar.md
  2. Contains 24-30 blog topics in calendar table
  3. Each topic maps to one of 4 Belfast pillars
  4. Pillar distribution is roughly balanced (5-8 per pillar)
  5. Contains MDX template, workflow, and distribution checklist sections
  </verify>
  <done>
  Content calendar with 24-30 Belfast blog topics planned across 12 weeks, mapped to 4 pillars, with publishing workflow, MDX template, and distribution checklist documented.
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation: `npx tsc --noEmit` passes
- Build: `npm run build` succeeds with 239 pages (no regressions)
- Existing blog posts load correctly (no field requirement changes)
- Keyword map UK section contains 19+ cluster entries
- Content calendar covers all 4 Belfast pillars with balanced distribution
</verification>

<success_criteria>
1. Blog infrastructure supports cluster metadata without breaking existing posts
2. UK keyword map is complete with all planned Belfast cluster entries
3. Content calendar provides clear roadmap for Plans 10-02 through 10-05
4. Zero TypeScript errors, zero build regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-belfast-blog-content-strategy/10-01-SUMMARY.md`
</output>

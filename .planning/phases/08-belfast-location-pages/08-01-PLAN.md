---
phase: 08-belfast-location-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/locales.ts
  - src/components/seo/structured-data.tsx
autonomous: true

must_haves:
  truths:
    - "Belfast LocalBusiness schema includes precise geo coordinates (54.5833, -5.9333)"
    - "Belfast address in locales.ts is split into structured fields (street, city, postal code, country)"
    - "LocalBusinessStructuredData renders enhanced schema for UK/IE locales with Belfast geo coordinates"
    - "NAP data in locales.ts matches verified Belfast GBP (1 Hollycroft Avenue, Belfast, BT5 5JE, +44 7722 432679)"
    - "Build succeeds with zero TypeScript errors after schema changes"
  artifacts:
    - path: "src/lib/locales.ts"
      provides: "Enhanced locale config with structured address fields and geo coordinates"
      contains: "latitude"
    - path: "src/components/seo/structured-data.tsx"
      provides: "Enhanced LocalBusinessStructuredData with geo, streetAddress, postalCode"
      contains: "GeoCoordinates"
  key_links:
    - from: "src/components/seo/structured-data.tsx"
      to: "src/lib/locales.ts"
      via: "import locales config"
      pattern: "locales\\[locale\\]"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/seo/structured-data.tsx"
      via: "LocalBusinessStructuredData component"
      pattern: "LocalBusinessStructuredData"
---

<objective>
Enhance Belfast location data infrastructure with structured address fields, geo coordinates, and improved LocalBusiness schema markup.

Purpose: The existing LocalBusiness schema uses flat address strings and lacks geo coordinates. Belfast-focused location pages require precise GeoCoordinates, structured PostalAddress (streetAddress, addressLocality, postalCode, addressCountry), and service area definitions to rank in local search and appear in Google's local pack. This plan builds the data foundation that Plan 08-02 will use for the Belfast location page.

Output: Enhanced locales.ts with structured address data and geo coordinates. Enhanced LocalBusinessStructuredData component with full PostalAddress, GeoCoordinates, and service area for Belfast (UK/IE) locales.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/locales.ts
@src/components/seo/structured-data.tsx
@src/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance locales.ts with structured address data and geo coordinates</name>
  <files>src/lib/locales.ts</files>
  <action>
    Extend the LocaleConfig interface in src/lib/locales.ts to add structured address fields and geo coordinates. This is needed because the current `address` field is a flat string ("1 Hollycroft Avenue, Belfast, BT5 5JE, United Kingdom") which can't be properly decomposed for PostalAddress schema markup.

    Add these fields to the LocaleConfig interface:
    ```typescript
    streetAddress?: string;     // Street address line (e.g., "1 Hollycroft Avenue")
    addressLocality: string;    // City/town (e.g., "Belfast")
    addressRegion?: string;     // State/province/region (e.g., "Northern Ireland")
    postalCode?: string;        // Postal/zip code (e.g., "BT5 5JE")
    geo?: {
      latitude: number;
      longitude: number;
    };
    ```

    Keep the existing `address` field as-is for backward compatibility (it's used in footer display).

    Update locale data with these values:

    **uk locale:**
    - streetAddress: "1 Hollycroft Avenue"
    - addressLocality: "Belfast"
    - addressRegion: "Northern Ireland"
    - postalCode: "BT5 5JE"
    - geo: { latitude: 54.5833, longitude: -5.9333 }

    **ie locale:**
    - streetAddress: "1 Hollycroft Avenue" (same Belfast office serves IE)
    - addressLocality: "Belfast"
    - addressRegion: "Northern Ireland"
    - postalCode: "BT5 5JE"
    - geo: { latitude: 54.5833, longitude: -5.9333 }

    **us locale:**
    - addressLocality: "Missouri" (extract from current flat address)
    - No streetAddress, postalCode, or geo (placeholder data, not verified GBP)

    **au locale:**
    - addressLocality: "Sydney" (extract from current flat address)
    - No streetAddress, postalCode, or geo (placeholder data)

    **nl locale:**
    - addressLocality: "Amsterdam" (extract from current flat address)
    - No streetAddress, postalCode, or geo (placeholder data)

    **dk locale:**
    - addressLocality: "Copenhagen" (extract from current flat address)
    - No streetAddress, postalCode, or geo (placeholder data)

    Make sure the existing `address` field stays intact. The new fields ADD to the config, not replace.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm TypeScript compiles with the new interface fields. Verify all 6 locale entries have addressLocality populated and UK/IE have full structured address + geo data.</verify>
  <done>LocaleConfig interface extended with structured address fields and geo coordinates. UK and IE locales have verified Belfast GBP data. Other locales have addressLocality extracted from flat address. All TypeScript types compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance LocalBusinessStructuredData with geo, structured PostalAddress, and service area</name>
  <files>src/components/seo/structured-data.tsx</files>
  <action>
    Update the LocalBusinessStructuredData component in src/components/seo/structured-data.tsx to use the new structured address fields from locales.ts.

    Changes to the LocalBusinessSchema interface:
    1. Update `address` to use full PostalAddress fields:
       ```typescript
       address: {
         "@type": "PostalAddress";
         streetAddress?: string;
         addressLocality: string;
         addressRegion?: string;
         postalCode?: string;
         addressCountry: string;
       };
       ```
    2. The `geo` field already exists as optional in the interface - populate it when locale has geo data:
       ```typescript
       geo?: {
         "@type": "GeoCoordinates";
         latitude: number;
         longitude: number;
       };
       ```
    3. Expand `areaServed` to include city-level for Belfast locales. For UK/IE, use an array:
       ```typescript
       areaServed: [
         { "@type": "City", name: "Belfast" },
         { "@type": "AdministrativeArea", name: "Northern Ireland" },
         { "@type": "Country", name: "United Kingdom" }
       ]
       ```
       For other locales, keep the existing Country-only pattern.

    Update the LocalBusinessStructuredData component function:
    1. Read new structured fields from locale config (streetAddress, addressLocality, addressRegion, postalCode, geo)
    2. Populate PostalAddress with structured fields (use addressLocality from new config, not splitting flat address string)
    3. Add geo coordinates when available (config.geo exists)
    4. Expand areaServed for UK/IE locales to include Belfast city-level targeting
    5. Update the LocalBusinessSchema TypeScript interface to match the new structure (areaServed becomes a union type or use `any` with justification comment for the array variant)

    IMPORTANT: Do NOT break the existing rendering. The component is used in src/app/[locale]/layout.tsx for ALL locales. Test that US, NL, DK, AU locales still get valid schema (they just won't have geo or streetAddress).

    Also update the `@id` for Belfast locales to include Belfast in the identifier for better schema disambiguation:
    - UK: `@id: "${BASE_URL}/uk#belfast-office"`
    - IE: `@id: "${BASE_URL}/ie#belfast-office"`
    - Other locales: keep existing `@id: "${BASE_URL}/${locale}"`
  </action>
  <verify>Run `npm run build` to confirm the build succeeds with enhanced schema. Check that the generated HTML for /uk/ locale includes GeoCoordinates, structured PostalAddress with streetAddress/postalCode, and expanded areaServed. Check that /us/ locale still builds correctly without geo data.</verify>
  <done>LocalBusinessStructuredData renders enhanced schema for UK/IE with Belfast geo coordinates (54.5833, -5.9333), full PostalAddress (streetAddress, postalCode, addressRegion), and city-level areaServed. Other locales continue rendering with existing Country-level schema. Build succeeds across all 6 locales.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds with 238+ pages
3. UK locale LocalBusiness schema includes:
   - streetAddress: "1 Hollycroft Avenue"
   - addressLocality: "Belfast"
   - postalCode: "BT5 5JE"
   - GeoCoordinates with latitude 54.5833
   - areaServed includes Belfast city
4. US locale LocalBusiness schema still renders correctly (no geo, Country-level areaServed)
5. NAP data matches verified Belfast GBP: name "Rosey Co. United Kingdom", address "1 Hollycroft Avenue, Belfast, BT5 5JE", phone "+44 7722 432679"
</verification>

<success_criteria>
- LocaleConfig has structured address fields and geo coordinates
- UK/IE locales have verified Belfast NAP data in structured format
- LocalBusinessStructuredData renders enhanced schema for Belfast with GeoCoordinates
- All other locales continue working without regression
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-belfast-location-pages/08-01-SUMMARY.md`
</output>

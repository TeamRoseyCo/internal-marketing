---
phase: 05-content-a-results-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/results-data.ts
  - src/components/results/case-study-card.tsx
  - src/components/results/client-logo-grid.tsx
  - src/app/[locale]/results/page-client.tsx
autonomous: true

must_haves:
  truths:
    - "Results page displays 4+ case studies with real metrics (before/after numbers)"
    - "Client logos appear with industry context (not bare logo grid)"
    - "Case study cards show Challenge, Solution, Results structure"
    - "All user-facing text uses translation system"
  artifacts:
    - path: "src/lib/results-data.ts"
      provides: "Case study and client logo data structures"
      min_lines: 80
      exports: ["caseStudies", "clientLogos", "CaseStudy", "ClientLogo"]
    - path: "src/components/results/case-study-card.tsx"
      provides: "Reusable case study card component"
      min_lines: 40
      exports: ["CaseStudyCard"]
    - path: "src/components/results/client-logo-grid.tsx"
      provides: "Client logo grid with metrics"
      min_lines: 30
      exports: ["ClientLogoGrid"]
  key_links:
    - from: "src/app/[locale]/results/page-client.tsx"
      to: "src/lib/results-data.ts"
      via: "imports case study data"
      pattern: "import.*caseStudies.*from.*results-data"
    - from: "src/app/[locale]/results/page-client.tsx"
      to: "src/components/results/case-study-card.tsx"
      via: "renders CaseStudyCard component"
      pattern: "import.*CaseStudyCard.*from"
---

<objective>
Create Results page foundation with real case study data and client logo grid.

Purpose: Replace placeholder case studies with real client metrics from migration-content. Add client logo grid with industry context following 2026 best practice (logos + metrics, not bare grid).

Output: Case study cards displaying Challenge > Solution > Results pattern with real before/after metrics, plus client logo trust bar with industry labels.
</objective>

<context>
Files to reference:
- src/app/[locale]/results/page-client.tsx (existing Results page)
- src/lib/page-translations.ts (translation pattern)
- migration-content/pages/Results.tsx (legacy case study data)
- .planning/phases/05-content-a-results-page/05-RESEARCH.md (patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create results data module with case studies and client logos</name>
  <files>src/lib/results-data.ts</files>
  <action>
  Create a new data module at src/lib/results-data.ts with:

  1. TypeScript interfaces:
     - CaseStudy: id, client, industry, icon (string for Lucide icon name), challenge, solution, metrics (array of {label, before, after}), testimonial (optional), videoId (optional)
     - ClientLogo: name, logoUrl, metric (optional), industry

  2. Export caseStudies array with 4+ entries using REAL data from migration-content/pages/Results.tsx:
     - Case Study 1: David Hayes / Babeltext (100+ leads in 48 hours, Technology/SaaS)
     - Case Study 2: Alvin Alexandro (21x ROAS, $600 spend, E-commerce)
     - Case Study 3: Restaurant Chain (300% ROAS, 250+ leads, $8.50 CPL)
     - Case Study 4: Professional Services / Legal (4x Revenue, 85 qualified calls, 12% conversion)

  3. Export clientLogos array with placeholder data structure (6 entries):
     - Each entry has name, logoUrl (use "/images/clients/placeholder-{n}.svg"), metric, industry
     - Mark with TODO comment: "Replace with real client logos when available"
     - Example metrics: "21x ROAS", "300% Growth", "100+ Leads"
     - Example industries: "E-commerce", "Technology", "Real Estate", "Legal"

  4. Include JSDoc comments explaining data structure

  Note: Use icon names as strings (e.g., "Briefcase", "ShoppingCart") - components will map to Lucide icons.
  </action>
  <verify>
  - File exists at src/lib/results-data.ts
  - TypeScript compiles without errors: npm run build (check for type errors)
  - Exports CaseStudy, ClientLogo, caseStudies, clientLogos
  </verify>
  <done>
  Data module exports typed case studies with real metrics and client logo placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CaseStudyCard and ClientLogoGrid components</name>
  <files>src/components/results/case-study-card.tsx, src/components/results/client-logo-grid.tsx</files>
  <action>
  Create two new components in src/components/results/:

  1. CaseStudyCard component (case-study-card.tsx):
     - "use client" directive
     - Props: CaseStudy type from results-data.ts
     - Layout: Three-column grid (Challenge | Solution | Results) on lg, stacked on mobile
     - Header: Icon + Client name + Industry badge
     - Results column: Map over metrics showing before -> after with arrow
     - Style: Use existing service-card pattern from page-client.tsx
     - Animation: Use motion.div with staggerItem variant for list items

  2. ClientLogoGrid component (client-logo-grid.tsx):
     - "use client" directive
     - Props: clients: ClientLogo[], translations for labels
     - Layout: 2-col mobile, 3-col md, 6-col lg grid
     - Each logo card: border with hover effect, grayscale -> color on hover
     - Show metric below logo if provided
     - Show industry label in small text
     - Use Next.js Image component for logos
     - Handle missing images gracefully (show placeholder div with company initial)

  Both components should:
  - Import from 'framer-motion' for animations
  - Use Tailwind classes consistent with existing Results page
  - Export as named exports
  </action>
  <verify>
  - Both files exist in src/components/results/
  - TypeScript compiles without errors
  - Components export correctly
  </verify>
  <done>
  Two reusable components ready for integration into Results page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate new components into Results page</name>
  <files>src/app/[locale]/results/page-client.tsx</files>
  <action>
  Update src/app/[locale]/results/page-client.tsx:

  1. Add imports at top:
     - Import { caseStudies, clientLogos } from "@/lib/results-data"
     - Import { CaseStudyCard } from "@/components/results/case-study-card"
     - Import { ClientLogoGrid } from "@/components/results/client-logo-grid"
     - Import icon mapping function (create inline or in results-data.ts)

  2. Replace the existing hardcoded caseStudies array (lines ~34-79) with imported data:
     - Remove the local caseStudies definition
     - Map imported caseStudies to include resolved icon components

  3. Replace the Trusted By section (lines ~115-128):
     - Replace placeholder text logos with ClientLogoGrid component
     - Pass clientLogos data and translation for "Trusted By"

  4. Update Case Studies section (lines ~146-210):
     - Replace inline card JSX with CaseStudyCard component
     - Map over imported caseStudies data
     - Pass each case study to CaseStudyCard component

  5. Ensure all text uses existing t.* translations where available

  Note: Keep existing animation variants (fadeInUp, staggerContainer, staggerItem).
  </action>
  <verify>
  - npm run build succeeds (214 pages generate)
  - Visit /us/results in dev mode - case studies display with real metrics
  - Client logo grid appears with placeholder logos
  - No hardcoded English text outside translation system
  </verify>
  <done>
  Results page displays real case study data with proper Challenge > Solution > Results structure.
  </done>
</task>

</tasks>

<verification>
Final verification before marking plan complete:

1. Build verification:
   ```bash
   npm run build
   ```
   Expected: 214 pages generate successfully, no TypeScript errors

2. Visual verification (dev mode):
   ```bash
   npm run dev
   ```
   - Visit http://localhost:3000/us/results
   - Verify case studies show real metrics (21x ROAS, 100+ leads, etc.)
   - Verify client logo grid appears with hover effects
   - Verify responsive layout (mobile, tablet, desktop)

3. Code quality:
   - No any types in new files
   - All components are client components ("use client")
   - Imports resolve correctly
</verification>

<success_criteria>
- [ ] src/lib/results-data.ts exports CaseStudy, ClientLogo types and data arrays
- [ ] src/components/results/case-study-card.tsx renders Challenge > Solution > Results
- [ ] src/components/results/client-logo-grid.tsx renders logos with metrics
- [ ] Results page imports and uses new components
- [ ] Case studies display real metrics from migration content
- [ ] npm run build succeeds with 214 pages
- [ ] All user-facing text uses translation system
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-a-results-page/05-01-SUMMARY.md`
</output>

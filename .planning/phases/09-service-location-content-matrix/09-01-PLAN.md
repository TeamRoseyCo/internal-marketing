---
phase: 09-service-location-content-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/pillars/uk/seo-belfast.mdx
  - src/content/pillars/uk/social-media-belfast.mdx
  - src/content/pillars/uk/paid-ads-belfast.mdx
  - src/content/pillars/uk/website-design-belfast.mdx
  - src/app/[locale]/[pillarSlug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Each Belfast pillar page links back to /uk/belfast/ location hub"
    - "Each Belfast pillar page links to its corresponding global service page"
    - "Each Belfast pillar page mentions Belfast office, phone, and address in CTA"
    - "Belfast pillar pages show related Belfast services via relatedPillars frontmatter"
    - "Non-Belfast pillar pages render unchanged (no regression)"
  artifacts:
    - path: "src/content/pillars/uk/seo-belfast.mdx"
      provides: "Enriched SEO Belfast pillar with office context, cross-links, local CTA"
      contains: "/uk/belfast/"
    - path: "src/content/pillars/uk/social-media-belfast.mdx"
      provides: "Enriched Social Media Belfast pillar with office context, cross-links, local CTA"
      contains: "/uk/belfast/"
    - path: "src/content/pillars/uk/paid-ads-belfast.mdx"
      provides: "Enriched Paid Ads Belfast pillar with office context, cross-links, local CTA"
      contains: "/uk/belfast/"
    - path: "src/content/pillars/uk/website-design-belfast.mdx"
      provides: "Enriched Website Design Belfast pillar with office context, cross-links, local CTA"
      contains: "/uk/belfast/"
    - path: "src/app/[locale]/[pillarSlug]/page.tsx"
      provides: "Belfast-aware CTA section and Related Services component"
      contains: "relatedPillars"
  key_links:
    - from: "src/content/pillars/uk/seo-belfast.mdx"
      to: "/uk/belfast/"
      via: "markdown link in content"
      pattern: "\\(/uk/belfast/\\)"
    - from: "src/content/pillars/uk/seo-belfast.mdx"
      to: "/uk/services/seo/"
      via: "markdown link to global service"
      pattern: "\\(/uk/services/seo/\\)"
    - from: "src/app/[locale]/[pillarSlug]/page.tsx"
      to: "pillar.relatedPillars"
      via: "conditional rendering of RelatedPillars section"
      pattern: "relatedPillars"
---

<objective>
Enrich all 4 Belfast pillar pages with bidirectional cross-links and Belfast office context, and update the pillar page route to render a "Related Services" section and Belfast-specific CTA.

Purpose: Completes the bidirectional internal linking network between Belfast location hub and pillar pages, improves PageRank distribution, and adds local context that differentiates Belfast pillars from generic content. The relatedPillars frontmatter field already exists but has no UI rendering - this plan activates it.

Output: 4 enriched Belfast pillar MDX files with cross-links and local context. Updated pillar page route with Related Services section and Belfast-aware CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-service-location-content-matrix/09-RESEARCH.md

@src/content/pillars/uk/seo-belfast.mdx
@src/content/pillars/uk/social-media-belfast.mdx
@src/content/pillars/uk/paid-ads-belfast.mdx
@src/content/pillars/uk/website-design-belfast.mdx
@src/app/[locale]/[pillarSlug]/page.tsx
@src/lib/content.ts
@src/app/[locale]/belfast/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich seo-belfast.mdx and social-media-belfast.mdx with cross-links and local context</name>
  <files>
    src/content/pillars/uk/seo-belfast.mdx
    src/content/pillars/uk/social-media-belfast.mdx
  </files>
  <action>
    FIRST: Read both MDX files in full to understand their current structure, headings, and content flow. Identify the exact location of:
    - The intro paragraph (after frontmatter)
    - The "Our Belfast [Service] Services" heading (or equivalent)
    - The FAQ section heading
    - The final generic CTA line (after the --- separator at the end)

    Then add 3 sections to each of these 2 Belfast pillar MDX pages. Do NOT change the frontmatter - it is already correct with relatedPillars configured. Do NOT remove existing content or [CONTENT TO BE WRITTEN IN PHASE 10] placeholders. Add these sections by integrating them into the existing content flow:

    **Section 1: Belfast Office Context** (add after the intro paragraph, before the "Our Belfast [Service] Services" heading or equivalent)
    Add a "Why Choose Rosey Co. for [Service] in Belfast?" section (or enhance the existing one if it has this heading) that:
    - Mentions the Belfast office at [1 Hollycroft Avenue](/uk/belfast/) with a link
    - References serving businesses across Belfast (Cathedral Quarter, Titanic Quarter, greater Belfast)
    - Mentions specializing in Belfast SMBs with budget range
    - Keep it to 2-3 short paragraphs. This is context, not full content (Phase 10 does that)

    **Section 2: Global Service Cross-Link** (add near the end, before the FAQ section heading)
    Add a short "Our Complete [Service] Offering" section that:
    - Links to the corresponding global service page:
      - seo-belfast -> /uk/services/seo/
      - social-media-belfast -> /uk/services/social-media/
    - Differentiates: "This page focuses on Belfast-specific strategies. See our [global offering](/uk/services/[service]/) for our full capabilities."
    - Keep to 1 short paragraph

    **Section 3: Belfast-Specific CTA** (replace the generic final CTA line at the very bottom of each file, after the last --- separator)
    Replace the generic "Contact Rosey Co..." line with a Belfast-specific CTA:

    For seo-belfast.mdx:
    ```
    ---

    Ready to dominate Belfast search results? [Visit our Belfast office](/uk/belfast/) or call [+44 7722 432679](tel:+447722432679) to schedule a free consultation. We're at 1 Hollycroft Avenue, Belfast, BT5 5JE - open Monday to Friday, 9:00 AM to 5:00 PM.
    ```

    For social-media-belfast.mdx:
    ```
    ---

    Ready to build a thriving social media presence in Belfast? [Visit our Belfast office](/uk/belfast/) or call [+44 7722 432679](tel:+447722432679) to schedule a free consultation. We're at 1 Hollycroft Avenue, Belfast, BT5 5JE - open Monday to Friday, 9:00 AM to 5:00 PM.
    ```

    IMPORTANT:
    - Do NOT add LocalBusiness schema to pillar pages (it belongs only on /uk/belfast/)
    - Do NOT change frontmatter fields
    - Keep NAP references as links to /uk/belfast/ rather than duplicating full address inline (except in the final CTA where it's a natural mention)
    - Use proper markdown link syntax: [text](/uk/belfast/) and [+44 7722 432679](tel:+447722432679)
  </action>
  <verify>
    For each of the 2 MDX files, verify:
    1. Contains link to /uk/belfast/ (grep for "/uk/belfast/")
    2. Contains link to corresponding global service page (grep for "/uk/services/")
    3. Contains tel: link to Belfast phone (grep for "tel:+447722432679")
    4. Frontmatter is unchanged (type: "pillar", relatedPillars array intact)
    5. No [CONTENT TO BE WRITTEN IN PHASE 10] placeholders were removed
    6. Run `npm run build` to confirm pages still build successfully
  </verify>
  <done>
    seo-belfast.mdx and social-media-belfast.mdx both contain bidirectional links to /uk/belfast/, links to their global service page counterpart, and Belfast-specific CTAs with phone number. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich paid-ads-belfast.mdx and website-design-belfast.mdx with cross-links and local context</name>
  <files>
    src/content/pillars/uk/paid-ads-belfast.mdx
    src/content/pillars/uk/website-design-belfast.mdx
  </files>
  <action>
    Apply the SAME pattern established in Task 1 to the remaining 2 Belfast pillar pages. FIRST: Read both MDX files in full to understand their structure. Then add the same 3 sections:

    **Section 1: Belfast Office Context** - Same pattern as Task 1 (after intro, before main services heading)
    **Section 2: Global Service Cross-Link** - Same pattern as Task 1 (before FAQ section heading), linking to:
      - paid-ads-belfast -> /uk/services/paid-ads/
      - website-design-belfast -> /uk/services/website-design/
    **Section 3: Belfast-Specific CTA** - Same pattern as Task 1 (replace generic CTA at bottom)

    For paid-ads-belfast.mdx:
    ```
    ---

    Ready to drive immediate results with Google Ads in Belfast? [Visit our Belfast office](/uk/belfast/) or call [+44 7722 432679](tel:+447722432679) to schedule a free consultation. We're at 1 Hollycroft Avenue, Belfast, BT5 5JE - open Monday to Friday, 9:00 AM to 5:00 PM.
    ```

    For website-design-belfast.mdx:
    ```
    ---

    Ready for a website that drives real results in Belfast? [Visit our Belfast office](/uk/belfast/) or call [+44 7722 432679](tel:+447722432679) to schedule a free consultation. We're at 1 Hollycroft Avenue, Belfast, BT5 5JE - open Monday to Friday, 9:00 AM to 5:00 PM.
    ```

    IMPORTANT: Same rules as Task 1 - no LocalBusiness schema, no frontmatter changes, no removing Phase 10 placeholders.
  </action>
  <verify>
    For each of the 2 MDX files, verify:
    1. Contains link to /uk/belfast/ (grep for "/uk/belfast/")
    2. Contains link to corresponding global service page (grep for "/uk/services/")
    3. Contains tel: link to Belfast phone (grep for "tel:+447722432679")
    4. Frontmatter is unchanged (type: "pillar", relatedPillars array intact)
    5. No [CONTENT TO BE WRITTEN IN PHASE 10] placeholders were removed
    6. Verify consistency with Task 1 output: all 4 files follow the same section structure
    7. Run `npm run build` to confirm all pages still build successfully
  </verify>
  <done>
    All 4 Belfast pillar pages now contain bidirectional links to /uk/belfast/, links to their global service page counterpart, and Belfast-specific CTAs with phone number. Consistent structure across all 4 files. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Related Services section and Belfast-aware CTA to pillar page route</name>
  <files>
    src/app/[locale]/[pillarSlug]/page.tsx
  </files>
  <action>
    BEFORE modifying, read src/app/[locale]/[pillarSlug]/page.tsx in full to verify the current structure. Identify:
    1. The `localeCode` variable (derived from `isValidLocale(locale)` near the top of the component)
    2. The `getPillarBySlug` import (already imported from `@/lib/content`)
    3. The Related Clusters section (a `<div className="mt-12">` containing `<RelatedClusters>`)
    4. The CTA section (a `<div className="mt-12 p-8 tech-card text-center">` containing "Ready to grow your business?")

    **Feature 1: Related Services Section**
    AFTER the Related Clusters section (the `<div>` containing `<RelatedClusters>`), and BEFORE the CTA section (the `<div>` with "Ready to grow your business?"), add a "Related Services" section that renders when `pillar.relatedPillars` has entries:

    1. `getPillarBySlug` is already imported from `@/lib/content`
    2. Look up related pillar metadata using `pillar.relatedPillars` array
    3. For each related pillar slug, call `getPillarBySlug(slug, localeCode)` to get title and excerpt
    4. Render a section with heading "Related Belfast Services" (only show if locale is 'uk' and pillar slug contains 'belfast')
    5. For non-Belfast pillars with relatedPillars, render heading "Related Services"
    6. Each related service shows as a card with title, excerpt snippet (first 120 chars), and link to `/${localeCode}/${slug}/`
    7. Style using existing `tech-card` class for consistency

    Implementation:
    ```tsx
    {/* Related Services */}
    {pillar.relatedPillars && pillar.relatedPillars.length > 0 && (
      <div className="mt-12">
        <h3 className="text-xl font-bold mb-6">
          {localeCode === 'uk' && pillarSlug.includes('belfast')
            ? 'Related Belfast Services'
            : 'Related Services'}
        </h3>
        <div className="grid sm:grid-cols-2 gap-4">
          {pillar.relatedPillars.map((relatedSlug) => {
            const related = getPillarBySlug(relatedSlug, localeCode);
            if (!related) return null;
            return (
              <Link
                key={relatedSlug}
                href={`/${localeCode}/${relatedSlug}/`}
                className="tech-card p-6 hover:border-primary/50 transition-all group"
              >
                <h4 className="font-semibold mb-2 group-hover:text-primary transition-colors">
                  {related.title.split('|')[0].trim()}
                </h4>
                <p className="text-sm text-muted-foreground">
                  {related.excerpt.length > 120
                    ? related.excerpt.slice(0, 120) + '...'
                    : related.excerpt}
                </p>
              </Link>
            );
          })}
        </div>
      </div>
    )}
    ```

    **Feature 2: Belfast-Aware CTA**
    Update the existing CTA section (the `<div className="mt-12 p-8 tech-card text-center">` containing heading "Ready to grow your business?") to be Belfast-specific when the pillar is a Belfast page:

    - If `localeCode === 'uk'` AND `pillarSlug.includes('belfast')`:
      - Heading: "Ready to grow your Belfast business?"
      - Subtext: "Visit our Belfast office or schedule a free consultation with our team."
      - Two buttons: "Get Started" linking to `/${localeCode}/contact` AND "Visit Belfast Office" linking to `/uk/belfast/`
      - Add phone CTA below: "Or call us: +44 7722 432679" with tel: link
    - Otherwise: Keep the existing generic CTA unchanged

    IMPORTANT:
    - Do NOT add LocalBusiness schema here
    - Do NOT modify the page metadata or schema sections
    - Use the existing `btn-hero` class for primary button and `border border-border bg-card hover:bg-accent` for secondary
    - Keep all existing functionality intact (breadcrumbs, TOC, MDX rendering, clusters, tags)
  </action>
  <verify>
    1. Run `npm run build` - must succeed with same page count (239 pages)
    2. Check that Belfast pillar pages (seo-belfast, etc.) render the "Related Belfast Services" section with cards linking to related pillars
    3. Check that Belfast pillar pages show Belfast-specific CTA with office link and phone
    4. Check that non-Belfast pillar pages (general US/AU/IE/NL/DK pillars) still show the generic CTA
    5. TypeScript compiles cleanly: `npx tsc --noEmit`
  </verify>
  <done>
    Pillar page route renders "Related Belfast Services" cards when relatedPillars frontmatter exists, and shows Belfast-specific CTA with office link and phone number for Belfast pillar pages. Non-Belfast pages unaffected.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build` completes with 239 pages
2. TypeScript: `npx tsc --noEmit` passes cleanly
3. All 4 Belfast MDX files contain links to /uk/belfast/ and their global service counterpart
4. Pillar page route renders Related Services section for Belfast pillar pages
5. Belfast CTA shows office link, phone number, and contact link
6. Non-Belfast pillar pages render identically to before (no regression)
7. No LocalBusiness schema on pillar pages (still only on /uk/belfast/)
</verification>

<success_criteria>
- Bidirectional internal linking complete: /uk/belfast/ -> pillars AND pillars -> /uk/belfast/
- Cross-links to global service pages exist on all 4 Belfast pillars
- Belfast-specific CTAs with tel: links on all 4 Belfast pillars
- relatedPillars frontmatter now has UI rendering (Related Services cards)
- Build succeeds, TypeScript clean, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-service-location-content-matrix/09-01-SUMMARY.md`
</output>

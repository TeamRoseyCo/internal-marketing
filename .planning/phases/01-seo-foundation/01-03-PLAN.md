---
phase: 01-seo-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/components/seo/structured-data.tsx
autonomous: true

must_haves:
  truths:
    - "Sitemap includes all locale routes with proper URLs"
    - "Robots.txt allows crawling of all locale paths"
    - "LocalBusiness schema renders with locale-specific address and phone"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Complete sitemap with all 213+ locale routes"
      contains: "roseyco.com"
    - path: "src/app/robots.ts"
      provides: "Robots configuration allowing all locale crawling"
      contains: "sitemap.xml"
    - path: "src/components/seo/structured-data.tsx"
      provides: "LocalBusiness schema per locale"
      contains: "LocalBusinessStructuredData"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/locales.ts"
      via: "import localeList"
      pattern: "localeList.*forEach"
---

<objective>
Verify and enhance sitemap, robots.txt, and LocalBusiness structured data for complete SEO coverage.

Purpose: Ensure Google can crawl all locale routes via sitemap, robots allows full access, and each locale has proper LocalBusiness schema for local search ranking.

Output: Verified SEO infrastructure ready for Google Search Console submission.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-seo-foundation/01-01-SUMMARY.md

# Files to verify/modify
@src/app/sitemap.ts
@src/app/robots.ts
@src/components/seo/structured-data.tsx
@src/lib/locales.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sitemap includes all locale routes</name>
  <files>src/app/sitemap.ts</files>
  <action>
Review and verify the existing sitemap.ts implementation.

Current implementation already loops through all locales and pages. Verify:

1. BASE_URL is correct: `https://roseyco.com`
2. All 6 locales are included (localeList from locales.ts)
3. All page paths are included:
   - Homepage (empty string)
   - /services
   - /services/seo
   - /services/social-media
   - /services/paid-ads
   - /services/website-design
   - /contact
   - /results
   - /privacy-policy
   - /blog
   - /blog/[slug] for all posts

Run build and check sitemap output:
```bash
npm run build
# Check that .next/server/app/sitemap.xml exists or view in browser at /sitemap.xml
```

If sitemap is correct, no changes needed. Document verification in summary.

If any issues found:
- Missing locales: Add to staticPages loop
- Wrong URLs: Fix BASE_URL or path construction
- Missing pages: Add to staticPages array
  </action>
  <verify>
Build succeeds.
Access /sitemap.xml and verify it includes:
- https://roseyco.com/us
- https://roseyco.com/au
- https://roseyco.com/uk
- https://roseyco.com/ie
- https://roseyco.com/nl
- https://roseyco.com/dk
And all service/page paths for each locale.
  </verify>
  <done>
Sitemap verified to include all 6 locales and all page routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify robots.txt configuration</name>
  <files>src/app/robots.ts</files>
  <action>
Review and verify the existing robots.ts implementation.

Current implementation should:
1. Allow all crawling with `userAgent: "*"` and `allow: "/"`
2. Block internal paths: /api/, /_next/, /private/
3. Point to sitemap at https://roseyco.com/sitemap.xml

Verify the configuration is correct:

```typescript
export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
        disallow: [
          "/api/",
          "/_next/",
          "/private/",
        ],
      },
    ],
    sitemap: `${BASE_URL}/sitemap.xml`,
  };
}
```

IMPORTANT: Do NOT add locale-specific rules. Google should be able to crawl all locales equally.

If current implementation is correct, no changes needed.
  </action>
  <verify>
Access /robots.txt and verify:
- Contains `Allow: /`
- Contains `Disallow: /api/`
- Contains `Sitemap: https://roseyco.com/sitemap.xml`
- Does NOT contain locale-specific blocks
  </verify>
  <done>
Robots.txt verified to allow crawling of all locale paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify LocalBusiness structured data</name>
  <files>src/components/seo/structured-data.tsx</files>
  <action>
Review and verify LocalBusinessStructuredData component.

Current implementation already:
1. Accepts locale parameter
2. Uses locales[locale] config for phone, address, country
3. Injects JSON-LD script with LocalBusiness schema

Verify the schema includes required fields for local SEO:

Required fields:
- @type: "LocalBusiness"
- @id: unique URL per locale
- name: "Rosey Co. {Country}"
- telephone: locale-specific phone
- address: locale-specific city/country
- areaServed: locale country
- url: locale-specific URL

The current implementation uses placeholder phone numbers which is acceptable for Phase 1 (noted in STATE.md).

Run build and verify structured data renders correctly:

1. Start dev server: `npm run dev`
2. Visit /us and /nl pages
3. View page source, search for "LocalBusiness"
4. Verify the @id, telephone, and address differ between locales

If all correct, no changes needed. Document verification.
  </action>
  <verify>
View page source for /us and /nl pages.
Verify LocalBusiness JSON-LD contains:
- Different @id URLs (/us vs /nl)
- Different telephone numbers
- Different address localities
- Different areaServed countries
  </verify>
  <done>
LocalBusiness structured data verified to render per-locale content.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Sitemap completeness:**
   - Access /sitemap.xml
   - Count total URLs (should be 200+)
   - Verify all 6 locales present
   - Verify all page types present for each locale

2. **Robots accessibility:**
   - Access /robots.txt
   - Confirm no locale blocking
   - Confirm sitemap reference is correct

3. **LocalBusiness per locale:**
   - View source on /us, /nl, /dk pages
   - Confirm different LocalBusiness data per locale
   - Validate with Google Rich Results Test: https://search.google.com/test/rich-results

4. **Build success:** `npm run build` completes without errors.
</verification>

<success_criteria>
- Sitemap includes all 213+ locale routes
- Robots.txt allows crawling with no locale-specific blocks
- LocalBusiness schema renders with correct per-locale data
- Build succeeds
- Ready for Google Search Console submission
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-foundation/01-03-SUMMARY.md`
</output>

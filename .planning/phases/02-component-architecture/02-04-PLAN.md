---
phase: 02-component-architecture
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/lib/translations.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Danish user sees grammatically correct text in browser with proper æ, ø, å"
    - "No instances of 'Vakst' remain (should be 'vækst')"
    - "No instances of 'Fa' for 'get' remain (should be 'Få')"
    - "No mixed English/Danish content"
    - "Currency formatting works for all 6 locales"
  artifacts:
    - path: "src/lib/translations.ts"
      provides: "Fixed Danish special characters"
      contains: "vækst"
  key_links:
    - from: "src/lib/translations.ts"
      to: "runtime"
      via: "UTF-8 encoding"
      pattern: "[æøå]"
---

<objective>
Fix Danish special character issues in translations.ts. Current translations use ASCII approximations (Vakst, Fa, pa) instead of proper Danish characters (vækst, Få, på). Verify currency formatting works correctly.

Purpose: Danish users see incorrect text. "Mere Vakst" should be "Mere vækst", "Fa flere leads" should be "Få flere leads". This is a credibility issue.

Output: All Danish translations use proper characters (æ, ø, å), currency formatting verified working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-component-architecture/02-CONTEXT.md

# Must read to fix
@src/lib/translations.ts
@src/lib/i18n/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Danish special characters throughout translations.ts</name>
  <files>
    src/lib/translations.ts
  </files>
  <action>
**FIRST: Verify the issue exists (baseline check):**
Run `grep -n "Vakst\|vakst\|Fa \| fa \| pa " src/lib/translations.ts` to confirm ASCII approximations exist.
- If grep returns results: proceed with fixes below
- If grep returns nothing: issue may already be fixed in 02-02/02-03, verify and skip to verification

**If issues exist, search and replace Danish ASCII approximations with proper UTF-8 characters:**

**Systematic replacements (case-sensitive):**
- `Vakst` -> `vækst` (growth)
- `vakst` -> `vækst`
- ` pa ` -> ` på ` (on/at - with spaces to avoid false positives)
- `Fa ` -> `Få ` (get - at start of sentence)
- ` fa ` -> ` få ` (get - mid-sentence)
- `Hojt` -> `Højt` (high)
- `hojt` -> `højt`
- `Sporgsmal` -> `Spørgsmål` (questions)
- `sporgsmal` -> `spørgsmål`
- `Hjaelper` -> `Hjælper` (helps)
- `hjaelper` -> `hjælper`
- `saetter` -> `sætter` (puts)
- `soger` -> `søger` (searches)
- `Sogeresultater` -> `Søgeresultater` (search results)
- `sogeresultater` -> `søgeresultater`
- `forlaengelse` -> `forlængelse` (extension)
- `Laer` -> `Lær` (learn)
- `laer` -> `lær`
- `Taeller` -> `Tæller` (counts/matters)
- `taeller` -> `tæller`
- `Omsaetning` -> `Omsætning` (revenue)
- `omsaetning` -> `omsætning`
- `malsrettet` -> `målrettet` (targeted)
- `malbar` -> `målbar` (measurable)
- `Malsrettet` -> `Målrettet`
- `baeredygtige` -> `bæredygtige` (sustainable)
- `vaekststrategier` -> `vækststrategier` (growth strategies)
- `forfaengelighed` -> `forfængelighed` (vanity)
- `praecis` -> `præcis` (precise)
- `besoegende` -> `besøgende` (visitors)
- `brandtilstedevaerelse` -> `brandtilstedeværelse` (brand presence)

**Verify these Danish words appear with correct characters:**
- vækst (growth) - multiple occurrences
- få (get) - used in CTAs
- på (on/at) - common preposition
- hjælper (helps)
- spørgsmål (questions)
- søger (searches)
- højt (high)

Open translations.ts, find the dk object, and fix all instances.
  </action>
  <verify>
    Run `grep -n "Vakst\|vakst" src/lib/translations.ts` - should return nothing
    Run `grep -n "vækst" src/lib/translations.ts` - should show Danish translations
    Run `npx tsc --noEmit` - no TypeScript errors
    File encoding must be UTF-8 (VS Code status bar shows "UTF-8")
  </verify>
  <done>
    All Danish translations use proper æ, ø, å characters with zero ASCII approximations
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify currency formatting across all locales</name>
  <files>
    src/lib/i18n/formatters.ts
  </files>
  <action>
Create a quick verification script or test the formatCurrency function:

1. Ensure formatCurrency handles all 6 locales correctly:
   - us: USD with $ symbol, thousand separator comma
   - au: AUD with $ symbol, thousand separator comma
   - uk: GBP with £ symbol, thousand separator comma
   - ie: EUR with € symbol
   - nl: EUR with € symbol, European formatting (period as thousand separator)
   - dk: DKK with kr. suffix, European formatting

2. Add a comment block in formatters.ts documenting expected outputs:
```typescript
// Expected formatCurrency(1299, locale) outputs:
// us: "$1,299.00"
// au: "A$1,299.00" or "$1,299.00" (depends on Intl)
// uk: "£1,299.00"
// ie: "€1,299.00"
// nl: "€ 1.299,00"
// dk: "1.299,00 kr."
```

3. Run quick test in dev console or Node:
```javascript
const { formatCurrency } = require('./src/lib/i18n/formatters');
console.log(formatCurrency(1299, 'us'));
console.log(formatCurrency(1299, 'uk'));
console.log(formatCurrency(1299, 'nl'));
console.log(formatCurrency(1299, 'dk'));
```

If any output is incorrect, adjust the Intl.NumberFormat configuration.

**Note on TRANS-06:** formatCurrency is created and verified in Phase 2. Actual UI usage will be demonstrated in Phase 5 (Content & Results Page) when pricing components are built. This task verifies the function works correctly - integration comes later.
  </action>
  <verify>
    Manual verification of formatCurrency outputs for all locales
    Build succeeds: `npm run build`
  </verify>
  <done>
    Currency formatting verified working for all 6 locales with correct symbols and separators. Function ready for Phase 5 integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final Phase 2 verification and cleanup</name>
  <files>
    (verification only - no new files)
  </files>
  <action>
Comprehensive verification of all Phase 2 requirements:

1. **TRANS-01 Verification (Header uses translation system):**
   - Open /nl in browser, verify header shows Dutch text
   - Open /dk in browser, verify header shows Danish text with proper characters
   - Check mobile menu on both locales

2. **TRANS-02 Verification (Footer uses translation system):**
   - Verify footer section headings are translated
   - Verify footer service links are translated
   - Verify brand description is translated
   - Verify copyright and tagline are translated

3. **TRANS-06 Verification (Currency formatting):**
   - Document that formatCurrency is ready for use in future components (Phase 5)
   - formatCurrency function verified working in Task 2

4. **TRANS-07 Verification (Build-time validation):**
   - Run `npx tsc --noEmit` - TypeScript catches type errors
   - Verify console.warn fires in dev for missing keys (if any)

5. **TRANS-08 Verification (Centralized system for Header/Footer):**
   - Grep for hardcoded strings in header.tsx: `grep -n "Services\|Contact\|Results\|Blog" src/components/layout/header.tsx`
   - Grep for hardcoded strings in footer.tsx: same pattern
   - Both should return only variable references, not string literals
   - **Scope note:** Phase 2 establishes the translation infrastructure and converts Header/Footer. Remaining components (service pages, homepage sections, etc.) will be converted in future phases as those components are worked on. This is the architectural foundation.

6. **Build verification:**
   - `npm run build` - 214 pages generate successfully
   - No TypeScript errors
   - No build warnings related to i18n
  </action>
  <verify>
    All TRANS requirements verified as complete (within Phase 2 scope)
    Build succeeds with zero errors
    No hardcoded strings remain in Header/Footer
  </verify>
  <done>
    Phase 2 Component Architecture requirements fully verified and complete
  </done>
</task>

</tasks>

<verification>
1. Danish character verification:
   - `grep -c "æ\|ø\|å" src/lib/translations.ts` returns high count
   - `grep -c "Vakst\|vakst" src/lib/translations.ts` returns 0
2. Currency formatting works for all 6 locales
3. Header displays correct translations in NL and DK (including mobile menu)
4. Footer displays correct translations in NL and DK
5. Build: `npm run build` succeeds
6. TypeScript: `npx tsc --noEmit` succeeds
7. Danish user viewing /dk sees grammatically correct text with proper characters
</verification>

<success_criteria>
- Zero ASCII approximations for Danish characters in translations.ts
- Danish user sees correct æ, ø, å characters in browser (user-observable)
- formatCurrency returns locale-appropriate formats (ready for Phase 5)
- TRANS-01, TRANS-02, TRANS-06, TRANS-07 verified complete
- TRANS-08 verified for Header/Footer (infrastructure established for future component conversion)
- Header and Footer have zero hardcoded English strings
- Build succeeds with 214 pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-04-SUMMARY.md`

This summary should include a "Phase Complete" section documenting:
- All requirements met (TRANS-01, TRANS-02, TRANS-06, TRANS-07, TRANS-08 for Header/Footer)
- Files created/modified
- Patterns established (useLocale, useTranslation, formatCurrency)
- Ready for Phase 3 (Translation QA) to do deeper quality review
- Note: formatCurrency demonstrated working, will be integrated in Phase 5
</output>
